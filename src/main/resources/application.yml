server:
  port: 8917

spring:
  application:
    name: config
  profiles:
    active:  native #配置文件的获取方式，从本地拿取配置文件
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared #配置文件的所在位置
